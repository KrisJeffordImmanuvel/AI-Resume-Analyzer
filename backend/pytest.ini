[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # Raised inside FastAPI's TestClient (a Starlette/httpx deprecation), not by this app.
    ignore:Using `httpx` with `starlette.testclient` is deprecated
