# Exact versions, so every install gets the same, tested packages.
# To upgrade: change a version here, run .\setup.ps1, then run pytest.
fastapi==0.141.1
uvicorn[standard]==0.53.0
sqlalchemy==2.0.54
alembic==1.20.0  # database migrations, applied automatically at start
psycopg[binary]==3.3.6  # Postgres driver, used only by the website (Neon); your PC uses SQLite
python-dotenv==1.2.3
python-multipart==0.0.32
httpx==0.28.1  # GitHub evidence check (also used by the test client)

# Resume parsing
pymupdf==1.28.2
python-docx==1.2.0

# AI (Gemini) and local semantic matching.
# sentence-transformers also installs PyTorch (CPU), a large one-time download.
google-genai==2.25.0
sentence-transformers==6.1.0

# Testing
pytest==9.1.1
