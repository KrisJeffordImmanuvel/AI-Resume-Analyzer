# Phase 0 dependencies. Parsing, AI and semantic-matching libraries
# (PyMuPDF, python-docx, google-genai, sentence-transformers) are added
# in the phases that first use them.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
python-dotenv>=1.0

# Testing
pytest>=8.0
httpx>=0.27
