# Only used by the development server (npm run dev). The normal app (start.ps1)
# ignores this file. Copy it to .env only if, during development, the backend
# is NOT running at http://localhost:8000.
# VITE_API_BASE_URL=http://localhost:8000
